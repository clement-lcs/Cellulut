#ifndef UIENGINE_H
#define UIENGINE_H

#include <QApplication>
#include "ui/controllers/SimulationController.h"

class UIEngine
{
private:
    static const int width  = 800;
    static const int height = 600;

    static UIEngine* _singleton;
    UIEngine();

    SimulationController* simulationController;
public:
    static UIEngine* getUIEngine(QApplication *app);
};

#endif // UIENGINE_H
